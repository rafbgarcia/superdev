<% content_for :user_dashboard do %>
  <%= simple_form_for current_user, url: update_password_users_path do |f| %>
    <%= f.input :password, label: 'Nova senha' %>
    <%= f.submit 'Salvar', class: 'btn btn-primary' %>
  <% end %>
<% end %>

<%= render 'template' %>
