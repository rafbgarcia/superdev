<%= simple_form_for @discussion, url: @discussion.persisted? && edit_discussion_path(@item, @discussion) do |f| %>
  <%= f.input :title, label: 'Título' %>
  <%= f.input :text, label: 'Texto' %>
  <%= f.submit 'Postar', class: 'btn btn-primary' %>
  <%= link_to 'Voltar', item_path(@item) %>
<% end %>
